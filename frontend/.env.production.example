# Backend URL — server-side only (no NEXT_PUBLIC prefix)
# Set this as an environment variable in your hosting platform (Vercel, Railway, etc.)
# Do NOT commit a .env.production file with real values.
API_URL=https://your-backend-domain.com
